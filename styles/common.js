import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  container: {
    paddingHorizontal: 8,
    paddingVertical: 20,
    flex: 1,
    backgroundColor: "#fff",
  },
});
